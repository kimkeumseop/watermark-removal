# 작업 맥락 기록

## 2026-07-28

- `BUILD_GUIDE.md`는 UTF-8 문서이며 PowerShell 기본 출력에서만 글자가 깨졌다. UTF-8로 다시 읽어 전체 요구사항을 확인했다.
- `vendor/`에는 지침서에 명시된 다섯 파일이 모두 있다. 이 폴더는 수정하거나 다시 다운로드하지 않는다.
- 기존 `D:\mannai\watermark-remover\templates\index.html`에서 실제 영상 픽셀 좌표로 영역을 저장하고 표시 크기에 맞춰 다시 그리는 방식을 참고한다.
- 사용자가 지정한 순수 HTML/CSS/JS 구조를 유지한다. 프레임워크, 패키지 매니저, CDN, 빌드 도구는 추가하지 않는다.
- 사각형 영역은 영상 고유 픽셀 좌표로 저장한다. 이 방식이면 창 크기가 바뀌어도 `renderRects()`가 현재 표시 크기에 맞춰 정확히 다시 그릴 수 있다.
- 단일 스레드 ffmpeg 코어만 사용하므로 COOP/COEP 헤더는 추가하지 않는다.
- 배포 설정 요청은 Netlify·Cloudflare Pages에서 큰 vendor 파일을 캐시하고 wasm MIME을 지정하는 파일 작성 범위로 해석한다. 외부 서비스에 실제 게시하는 작업은 포함하지 않는다.
- 화면은 업로드, 영역 선택, 처리, 완료 패널로 나눴다. 현재 단계만 표시해 작업 흐름을 단순하게 유지한다.
- 결과 재편집은 ffmpeg 코어를 다시 로드하지 않고 기존 원본과 선택 영역을 유지한다. 다른 영상을 선택할 때만 원본·결과 Blob URL과 상태를 정리한다.
- 200MB 초과 파일은 차단하지 않고 지침대로 경고만 표시한다.
- 선택 오버레이와 영상 요소의 경계가 항상 같도록 편집 영상에는 `object-fit`이나 최대 높이를 적용하지 않는다. 이 조건이 화면 좌표와 실제 영상 좌표의 비례를 보장한다.
- `node --check`로 JavaScript 문법을 확인했다. 임시 로컬 HTTP 서버 검사에서 HTML, CSS, JS, Worker 청크, 코어 JS, wasm이 모두 200으로 제공됐고 wasm MIME은 `application/wasm`이었다.
- DOM ID 참조 23개가 모두 HTML에 존재함을 확인했다. 모의 DOM·ffmpeg 통합 검사에서 640×360 영상 좌표 변환, 프레임 클램프, 여러 `delogo` 필터 연결, MP4 결과 흐름이 통과했다.
- 실제 브라우저 검증을 위해 연결을 시도했으나 현재 실행 환경에 사용 가능한 브라우저가 없었다. 별도 브라우저 자동화 도구로 우회하지 않고 이 제한을 명시한다.
