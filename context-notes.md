# 작업 맥락 기록

## 2026-07-28

- `BUILD_GUIDE.md`는 UTF-8 문서이며 PowerShell 기본 출력에서만 글자가 깨졌다. UTF-8로 다시 읽어 전체 요구사항을 확인했다.
- `vendor/`에는 지침서에 명시된 다섯 파일이 모두 있다. 이 폴더는 수정하거나 다시 다운로드하지 않는다.
- 기존 `D:\mannai\watermark-remover\templates\index.html`에서 실제 영상 픽셀 좌표로 영역을 저장하고 표시 크기에 맞춰 다시 그리는 방식을 참고한다.
- 사용자가 지정한 순수 HTML/CSS/JS 구조를 유지한다. 프레임워크, 패키지 매니저, CDN, 빌드 도구는 추가하지 않는다.
- 사각형 영역은 영상 고유 픽셀 좌표로 저장한다. 이 방식이면 창 크기가 바뀌어도 `renderRects()`가 현재 표시 크기에 맞춰 정확히 다시 그릴 수 있다.
- 단일 스레드 ffmpeg 코어만 사용하므로 COOP/COEP 헤더는 추가하지 않는다.
- 배포 설정 요청은 Netlify·Cloudflare Pages에서 큰 vendor 파일을 캐시하고 wasm MIME을 지정하는 파일 작성 범위로 해석한다. 외부 서비스에 실제 게시하는 작업은 포함하지 않는다.
- 화면은 업로드, 영역 선택, 처리, 완료 패널로 나눴다. 현재 단계만 표시해 작업 흐름을 단순하게 유지한다.
- 결과 재편집은 ffmpeg 코어를 다시 로드하지 않고 기존 원본과 선택 영역을 유지한다. 다른 영상을 선택할 때만 원본·결과 Blob URL과 상태를 정리한다.
- 200MB 초과 파일은 차단하지 않고 지침대로 경고만 표시한다.
- 선택 오버레이와 영상 요소의 경계가 항상 같도록 편집 영상에는 `object-fit`이나 최대 높이를 적용하지 않는다. 이 조건이 화면 좌표와 실제 영상 좌표의 비례를 보장한다.
- `node --check`로 JavaScript 문법을 확인했다. 임시 로컬 HTTP 서버 검사에서 HTML, CSS, JS, Worker 청크, 코어 JS, wasm이 모두 200으로 제공됐고 wasm MIME은 `application/wasm`이었다.
- DOM ID 참조 23개가 모두 HTML에 존재함을 확인했다. 모의 DOM·ffmpeg 통합 검사에서 640×360 영상 좌표 변환, 프레임 클램프, 여러 `delogo` 필터 연결, MP4 결과 흐름이 통과했다.
- 실제 브라우저 검증을 위해 연결을 시도했으나 현재 실행 환경에 사용 가능한 브라우저가 없었다. 별도 브라우저 자동화 도구로 우회하지 않고 이 제한을 명시한다.

## 2026-07-28 Vercel 배포 개선

- 배포된 메인 페이지와 모든 ffmpeg 자원이 200으로 응답하며 wasm MIME도 `application/wasm`이다.
- Vercel의 기본 `Cache-Control`이 `public, max-age=0, must-revalidate`라 약 31MB인 wasm이 방문 때마다 재검증된다. 공식 Vercel 설정 방식인 루트 `vercel.json`으로 vendor 자원에 1년 장기 캐시를 적용한다.
- 현재 선택 오버레이가 영상 전체를 덮기 때문에 네이티브 영상 컨트롤을 포인터로 조작하기 어렵다. 편집 화면 밖에 별도 재생·처음으로 버튼을 추가해 영역 선택을 방해하지 않고 영상을 확인할 수 있게 한다.
- 편집 단계에서 선택한 파일명·해상도·용량을 보여 사용자가 올바른 영상을 고른 상태인지 바로 확인할 수 있게 한다.
- 작업 폴더에 있던 관련 없는 스크린샷 5장은 사용자 요청에 따라 삭제했다. Git에 추적되지 않은 파일이어서 커밋에는 삭제 기록이 생기지 않는다.
- `app.js` 문법, `vercel.json` 형식과 헤더 값, HTML의 DOM ID 28개를 검사했다. 모의 통합 검사에서 좌표 변환, 프레임 클램프, delogo 체인, 결과 생성, 새 미리보기 제어가 모두 통과했다.
- 커밋 `d2fa1bd`를 GitHub `main`에 푸시했다. Vercel 배포 주소에서 새 `playPauseButton` 코드가 제공되고 wasm 응답에 `Cache-Control: public, max-age=31536000, immutable`이 적용된 것을 확인했다.

## 2026-07-29 애드센스 연동

- 게시자 ID는 각 페이지의 `<meta name="google-adsense-account">` 한 곳에만 둔다. `ads.js`가 이 값을 읽어 로더 스크립트를 주입하므로 ID를 여러 파일에 중복해 적을 필요가 없다. 이 메타 태그는 구글이 문서화한 사이트 소유 확인 방법이기도 하다.
- `ads.js`는 게시자 ID가 `ca-pub-` + 숫자 16자리 형식이 아니거나 광고 단위 ID가 비어 있으면 광고 자리를 DOM에서 제거한다. 설정 전에 빈 상자나 콘솔 오류가 남지 않게 하려는 의도다.
- 광고 자리는 작업 영역 아래와 FAQ 아래 두 곳이다. 영상 스테이지나 버튼 근처는 오클릭 유도로 판정될 수 있어 피했다.
- 애드센스는 콘텐츠가 얇은 사이트를 반려하는 경우가 많다. 승인 가능성을 높이려고 FAQ 5문항과 `privacy.html`, `terms.html`을 추가하고 푸터에서 연결했다. 개인정보처리방침에는 광고 쿠키와 거부 방법을 명시했다.
- 문의 이메일은 전역 설정에 있는 사용자 주소를 사용했다. 다른 주소를 쓰려면 두 약관 페이지에서 바꿔야 한다.
- EEA·영국 대상 동의 메시지는 애드센스 대시보드에서 설정하는 항목이라 코드로 처리하지 않는다.
- 프레임워크·빌드 도구를 추가하지 않는 기존 제약을 유지했다. `ads.js`는 의존성 없는 즉시 실행 함수 하나다.
- `ads.js`를 모의 DOM에서 네 가지 상황(플레이스홀더, 메타 없음, 정상 ID, 공백 포함)으로 검사해 20건이 모두 통과했다. HTML 태그 균형, `app.js` 선택자 참조 28개, CSS 중괄호 균형도 확인했다.
- 로컬 서버에서 `/`, `/privacy.html`, `/terms.html`, `/ads.txt`, `/ads.js`가 모두 200이며 `ads.txt`는 `text/plain`으로 제공됐다.
- 브라우저 확장이 연결되지 않아 이번에도 실제 화면 검증은 하지 못했다. 우회하지 않고 제한을 명시한다.
- 사용자가 게시자 ID `ca-pub-1059415497859090`을 전달해 세 페이지의 메타 태그와 `ads.txt`에 반영했다. 실제 값으로 `ads.js`를 돌려 로더 URL이 사용자가 준 스니펫과 같은 `?client=ca-pub-1059415497859090`으로 생성되는 것을 확인했다.
- 광고 단위 ID는 아직 없어 `data-ad-slot`이 비어 있다. 이 상태에서 광고 자리 두 곳은 제거되고 로더만 로드되므로 심사에는 지장이 없다.
- 애드센스에서 사이트 확인이 실패했다. 확인 크롤러는 HTML 원본에서 스니펫을 찾으므로 `ads.js`가 로더를 주입하는 방식은 신뢰할 수 없다. 로더 `<script>`를 세 페이지의 `<head>`에 그대로 넣고 `ads.js`는 광고 단위 렌더링만 담당하도록 바꿨다. 이 로더는 앞으로 JavaScript로 주입하지 않는다.
- Vercel의 Deployment Protection이 켜져 있으면 구글 크롤러가 페이지에 접근하지 못해 확인이 실패한다. 코드로 해결할 수 없는 항목이라 README의 점검 순서에 적어 두었다.
